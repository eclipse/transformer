subprojects {
  repositories {
    mavenCentral()
    maven {
      url uri('https://bndtools.jfrog.io/bndtools/libs-snapshot-local')
    }
  }
}

tasks.named('wrapper') {
  jarFile = rootProject.file('.gradle-wrapper/gradle-wrapper.jar')
}
